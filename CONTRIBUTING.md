# 贡献指南

感谢你考虑为 xxm-test-js 做出贡献！这份文档将帮助你了解如何参与项目开发。

## 📋 目录

- [行为准则](#行为准则)
- [如何贡献](#如何贡献)
- [开发流程](#开发流程)
- [代码规范](#代码规范)
- [提交规范](#提交规范)
- [文档编写](#文档编写)

---

## 行为准则

我们致力于为每个人提供友好、安全和热情的环境。请遵守以下原则：

- 使用友好和包容的语言
- 尊重不同的观点和经验
- 优雅地接受建设性批评
- 关注对社区最有利的事情
- 对其他社区成员表示同情

---

## 如何贡献

### 报告 Bug

如果你发现了 bug，请在 GitHub 上创建一个 Issue，并包含以下信息：

- **清晰的标题**：简明扼要地描述问题
- **重现步骤**：详细说明如何重现问题
- **预期行为**：描述你期望发生的行为
- **实际行为**：描述实际发生的行为
- **环境信息**：
  - Node.js 版本
  - npm/yarn 版本
  - 操作系统
  - 浏览器版本（如适用）
- **相关代码**：提供最小可复现的代码示例

**Bug 报告模板：**

```markdown
## Bug 描述

[清晰简洁的描述]

## 重现步骤

1. ...
2. ...
3. ...

## 预期行为

[描述预期的结果]

## 实际行为

[描述实际的结果]

## 环境信息

- xxm-test-js 版本:
- Node.js 版本:
- 浏览器:
- 操作系统:

## 相关代码

\`\`\`javascript
// 你的代码
\`\`\`
```

---

### 提出新功能

如果你有新功能建议，请创建一个 Issue，包含：

- **功能描述**：详细说明你的想法
- **使用场景**：说明为什么需要这个功能
- **API 设计**：如果可能，提供 API 设计示例
- **替代方案**：是否考虑过其他实现方式

**功能请求模板：**

```markdown
## 功能描述

[清晰简洁的描述]

## 使用场景

[说明什么场景需要这个功能]

## API 设计建议

\`\`\`javascript
// 示例代码
\`\`\`

## 替代方案

[是否有其他实现方式]
```

---

### 提交代码

1. **Fork 项目**到你的 GitHub 账号
2. **克隆**你 fork 的仓库到本地
3. **创建分支**：`git checkout -b feature/your-feature-name`
4. **编写代码**并确保通过测试
5. **提交代码**：遵循提交规范
6. **推送分支**：`git push origin feature/your-feature-name`
7. **创建 Pull Request**

---

## 开发流程

### 环境准备

1. 确保安装了 Node.js（>= 14.x）和 npm
2. Clone 项目：
   ```bash
   git clone https://github.com/xuxiaomao123/xxm-test-js.git
   cd xxm-test-js
   ```
3. 安装依赖：
   ```bash
   npm install
   ```

### 项目结构

```
xxm-test-js/
├── src/              # 源代码
│   ├── index.ts      # 主入口文件
│   └── lib/          # 工具函数
├── dist/             # 编译输出
├── docs/             # 文档
│   ├── README.md
│   └── zh/           # 中文文档
├── tests/            # 测试文件（待建立）
├── package.json
├── tsconfig.json
└── README.md
```

### 开发新功能

#### 1. 创建函数文件

在 `src/lib/` 目录下创建新的 TypeScript 文件，例如 `yourFunction.ts`：

````typescript
/**
 * 函数简短描述
 * Copyright (c) 2024 xxm
 *
 * @param {Type} param - 参数描述
 * @returns {ReturnType} 返回值描述
 *
 * @example
 * ```typescript
 * const result = yourFunction(param);
 * console.log(result);
 * ```
 */
export function yourFunction(param: any): any {
  // 实现代码
}
````

#### 2. 导出函数

在 `src/index.ts` 中导入并导出你的函数：

```typescript
import { yourFunction } from './lib/yourFunction';

// 添加到 JSDoc 注释
// 添加到 xxmJs 对象
// 添加到 export 列表
```

#### 3. 编写测试

在 `tests/` 目录下创建对应的测试文件：

```typescript
import { yourFunction } from '../src/lib/yourFunction';

describe('yourFunction', () => {
  it('should do something', () => {
    const result = yourFunction('test');
    expect(result).toBe('expected');
  });

  it('should handle edge cases', () => {
    expect(() => yourFunction(null)).toThrow();
  });
});
```

#### 4. 编写文档

在 `docs/zh/` 目录下创建 Markdown 文档：

```markdown
# 函数名称

## 概述

[函数功能描述]

## 版权信息

版权所有 (c) 2024 xxm

## 函数签名

\`\`\`typescript
function yourFunction(param: Type): ReturnType;
\`\`\`

## 参数

- `param (Type)`: 参数描述

## 返回值

- 返回描述

## 示例用法

\`\`\`javascript
// 示例代码
\`\`\`

## 引入

\`\`\`javascript
import { yourFunction } from 'xxm-test-js';
\`\`\`
```

#### 5. 更新文档配置

在 `docs/.vuepress/config.js` 中添加新页面到侧边栏。

#### 6. 编译和测试

```bash
# 编译 TypeScript
npm run build

# 运行测试（待配置）
npm test

# 本地预览文档
npm run docs:dev
```

---

## 代码规范

### TypeScript 规范

- 使用 TypeScript 编写所有代码
- 为所有函数参数和返回值添加类型注解
- 优先使用接口（interface）而非类型别名（type）
- 使用泛型提高代码复用性

**示例：**

```typescript
// ✅ 好的做法
export function processArray<T>(items: T[], callback: (item: T) => boolean): T[] {
  return items.filter(callback);
}

// ❌ 不好的做法
export function processArray(items: any, callback: any): any {
  return items.filter(callback);
}
```

### JSDoc 注释

所有导出的函数必须包含完整的 JSDoc 注释：

````typescript
/**
 * 函数简短描述
 *
 * 详细描述（可选）
 *
 * @param {Type} param - 参数描述
 * @returns {ReturnType} 返回值描述
 *
 * @example
 * ```typescript
 * const result = functionName(param);
 * ```
 *
 * @throws {Error} 错误情况描述
 */
````

### 命名规范

- **函数名**：小驼峰命名（camelCase），动词开头
  - `getUserInfo`, `formatDate`, `isValid`
- **类名**：大驼峰命名（PascalCase），名词
  - `FormValidator`, `TimeThread`, `WebSocketClient`
- **常量**：大写下划线（UPPER_SNAKE_CASE）
  - `MAX_SIZE`, `DEFAULT_TIMEOUT`
- **私有变量**：下划线前缀（可选）
  - `_privateVar`

### 代码风格

- 使用 2 空格缩进
- 字符串使用单引号
- 语句末尾不加分号（待 Prettier 配置后统一）
- 每行最大长度 100 字符
- 函数最大行数 50 行（复杂函数应拆分）

---

## 提交规范

### Commit Message 格式

遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type 类型：**

- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响代码运行）
- `refactor`: 重构（既不是新增功能，也不是修复 bug）
- `perf`: 性能优化
- `test`: 增加测试
- `chore`: 构建过程或辅助工具的变动
- `revert`: 回滚到上一个版本

**示例：**

```bash
# 新增功能
git commit -m "feat(array): add flattenDeep function"

# Bug 修复
git commit -m "fix(time): fix handelTime timezone issue"

# 文档更新
git commit -m "docs(readme): update installation guide"

# 重构
git commit -m "refactor(utils): simplify isType implementation"
```

### 分支命名

- `feature/功能名称`：新功能开发
- `fix/问题描述`：Bug 修复
- `docs/文档说明`：文档更新
- `refactor/重构说明`：代码重构

**示例：**

```bash
feature/add-storage-utils
fix/debounce-memory-leak
docs/add-contributing-guide
refactor/optimize-deep-copy
```

---

## 文档编写

### 文档结构

每个函数的文档应包含：

1. **标题**：函数名称
2. **概述**：功能简介
3. **版权信息**
4. **函数签名**：TypeScript 类型定义
5. **参数说明**：每个参数的详细说明
6. **返回值说明**
7. **示例用法**：至少 2 个实际场景
8. **适用场景**（可选）
9. **注意事项**（可选）
10. **引入方式**

### 示例代码质量

- 使用真实、有意义的变量名
- 展示常见使用场景
- 包含边界情况处理
- 代码简洁易懂

---

## Pull Request 检查清单

提交 PR 前，请确保：

- [ ] 代码已通过编译（`npm run build`）
- [ ] 代码已通过所有测试（`npm test`）
- [ ] 添加了新功能的测试用例
- [ ] 更新了相关文档
- [ ] 遵循了代码规范
- [ ] Commit message 符合规范
- [ ] 解决了所有合并冲突
- [ ] PR 描述清晰，说明了改动内容

---

## 获得帮助

如果你在贡献过程中遇到问题：

1. 查看项目文档和 Issue
2. 在 GitHub Discussions 提问
3. 发送邮件给维护者

---

## 许可证

通过向该项目提交代码，你同意你的贡献将在 ISC 许可证下发布。

---

**感谢你的贡献！** 🎉
